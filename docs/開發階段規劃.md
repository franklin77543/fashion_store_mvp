# Fashion Store MVP - 開發路線圖

## 📋 開發策略

採用由下而上 (Bottom-Up) 的開發方式：

1. 先建立穩固的資料層
2. 再建立可靠的 API 層
3. 然後整合 AI 功能
4. 最後開發前端介面

這樣可以確保每一層都經過充分測試，降低後續整合問題。

---

## 🎯 Phase 1: 資料層建立 (Data Layer)

**目標**: 建立完整的資料庫結構，並從 Fashion Dataset 匯入商品資料

### ✅ Task 1.1: 建立專案結構
- ✅ 建立 `backend/` 目錄結構
- ✅ 建立所有必要的子目錄 (models, repositories, services, api, schemas, core, db, scripts)
- ✅ 建立 `__init__.py` 檔案

### ✅ Task 1.2: 設定 Python 開發環境
- ✅ 建立 `requirements.txt`
  ```
  fastapi==0.115.0
  uvicorn[standard]==0.32.0
  python-multipart==0.0.9
  sqlalchemy==2.0.35
  pandas>=2.0.0
  python-dotenv==1.0.1
  pydantic-settings==2.6.1
  pytest==7.4.3
  pytest-asyncio==0.21.1
  pillow>=10.0.0
  ```
- ✅ 建立 Python 虛擬環境
  ```powershell
  cd backend
  python -m venv venv
  .\venv\Scripts\Activate.ps1
  pip install -r requirements.txt
  ```
- 建立 `.env.example` 檔案範本
- 建立 `.env` 檔案 (從 .env.example 複製)
  ```bash
  # Database
  DATABASE_URL=sqlite:///./fashion_store.db
  
  # App Settings
  APP_NAME=Fashion Store
  API_V1_PREFIX=/api/v1
  ENVIRONMENT=development
  DEBUG=True
  HOST=0.0.0.0
  PORT=8000
  
  # CORS
  BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
  
  # Image Settings
  IMAGE_BASE_PATH=../fashion-dataset/images
  ```

### ⏳ Task 1.3: 分析 CSV 來源資料
- 使用 Python 和 pandas 分析 `styles.csv` 檔案結構
  ```python
  import pandas as pd
  df = pd.read_csv('../fashion-dataset/styles.csv')
  print(df.head())
  print(df.info())
  print(df.columns)
  ```
- 記錄所有欄位名稱與資料型態
  - `id` - 商品唯一識別碼 (Integer)
  - `gender` - 性別分類 (String)
  - `masterCategory` - 主要類別 (String)
  - `subCategory` - 子類別 (String)
  - `articleType` - 商品類型 (String)
  - `baseColour` - 基礎顏色 (String)
  - `season` - 適用季節 (String)
  - `year` - 年份 (Integer)
  - `usage` - 使用場合 (String)
  - `productDisplayName` - 商品顯示名稱 (String)
- 分析 `styles/*.json` 檔案結構
  - 記錄額外欄位 (price, discountedPrice, brandName 等)
- 統計資料分布
  - 性別分布
  - 類別分布
  - 顏色分布
  - 價格範圍
- 識別需要清洗的資料
  - 缺失值處理策略
  - 顏色名稱標準化規則
  - 圖片路徑驗證

### ⏳ Task 1.4: 設計資料庫 ER Diagram 與 DBML
- 根據 CSV 欄位設計資料表結構
- 建立 `docs/models.dbml` 檔案
  - 定義所有資料表 (products, categories, genders, colors, brands 等)
  - 定義欄位與資料型態
  - 定義主鍵 (Primary Key)
  - 定義外鍵關聯 (Foreign Key)
  - 定義索引 (Index)
- 前往 https://dbdiagram.io 貼上 DBML
- 調整圖表佈局
- 匯出 ER Diagram 為 SVG 檔案
- 儲存為 `docs/er_diagram.svg`

### ⏳ Task 1.5: 建立資料庫 Models
根據 DBML 設計建立 SQLAlchemy Models
`models/product_model.py` - Product 商品主表
`models/master_category_model.py` - MasterCategory 主分類表
`models/gender_model.py` - Gender 性別表
`models/color_model.py` - Color 顏色表
`models/brand_model.py` - Brand 品牌表 (可選)
`models/order_model.py` - Order 訂單表 (基礎版)
`models/order_item_model.py` - OrderItem 訂單項目表

### ⏳ Task 1.6: 建立資料庫連線
- `db/session.py` - Database session 和 Base
  - SQLite 設定: `check_same_thread=False`
  - `get_db()` 依賴注入
- `core/config.py` - 設定檔管理
  - 使用 pydantic-settings
  - 環境變數載入
  - CORS origins 處理 (字串 → 列表)

### ⏳ Task 1.7: 建立資料匯入腳本
- `scripts/import_fashion_data.py` - Fashion Dataset 匯入腳本
  - 讀取 `styles.csv` 和 `styles/*.json`
  - 資料清洗與驗證
  - 處理缺失值
  - 標準化顏色名稱
  - 匯入商品、類別、顏色等資料
  - 驗證圖片檔案存在性

### ⏳ Task 1.8: 執行資料匯入
```powershell
cd backend
# 確保虛擬環境已啟動
.\venv\Scripts\Activate.ps1

# 執行匯入
python scripts\import_fashion_data.py
```


### ⏳ Task 1.9: 驗證資料完整性
- `scripts/analyze_data.py` - 初步資料分析腳本
- `scripts/verify_import.py` - 資料匯入後驗證腳本
  - 商品資料完整性檢查
  - 類別分布分析
  - 顏色分布統計
  - 價格範圍驗證
  - 圖片檔案檢查
  - 隨機商品範例展示

**完成標準:**
- ✅ 資料庫包含 ~44,000 件商品
- ✅ 所有關聯表資料正確
- ✅ 圖片路徑正確
- ✅ 資料完整性 > 95%

---

## 🎯 Phase 2: Backend API 建立 (API Layer)

**目標**: 建立基礎的 RESTful API，提供商品查詢功能


### ⏳ Task 2.1: 建立 Pydantic Schemas (請求/回應模型)
  - `Product` - 商品基本資訊
  - `ProductDetail` - 商品詳細資訊
  - `ProductList` - 商品列表回應
 `schemas/master_category_schema.py` (MasterCategory)

### ⏳ Task 2.2: 建立 Repository Layer (資料庫操作)
 `repositories/master_category_repository.py`
   - `get_all_master_categories()`
   - `get_master_category_by_id()`
- `repositories/order_repository.py`
  - `create_order()`
  - `get_order_by_id()`

### ⏳ Task 2.3: 建立 Service Layer (業務邏輯)
- `services/product_service.py`
  - `get_product_detail()` - 取得商品完整資訊
  - `list_products()` - 分頁列表
  - `search_products()` - 搜尋功能
  - `filter_products()` - 篩選功能

### ⏳ Task 2.4: 建立 API Endpoints
- `api/product_api.py`
  - `GET /api/v1/products` - 商品列表 (分頁)
  - `GET /api/v1/products/{id}` - 商品詳情
  - `GET /api/v1/products/search?q=xxx` - 搜尋商品
  - `GET /api/v1/products/filter` - 進階篩選
- `api/category_api.py`
  - `GET /api/v1/master-categories` - 所有主分類
- `api/health_api.py`
  - `GET /api/v1/health` - 健康檢查

### ⏳ Task 2.5: 建立依賴注入
- `dependencies.py` - 統一管理 Repository 和 Service 的依賴注入

### ⏳ Task 2.6: 建立 FastAPI 應用
- `main.py`
  - 初始化 FastAPI
  - 設定 CORS
  - 註冊所有路由
  - 建立資料庫表
  - 設定靜態檔案服務 (圖片)

### ⏳ Task 2.7: 啟動 Backend 伺服器
```powershell
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### ⏳ Task 2.8: 測試 API 端點
使用 Postman 或 Thunder Client 測試：
- `GET http://localhost:8000/api/v1/health`
- `GET http://localhost:8000/api/v1/products?page=1&limit=20`
- `GET http://localhost:8000/api/v1/products/{id}`
- `GET http://localhost:8000/api/v1/products/search?q=shirt`
- `GET http://localhost:8000/api/v1/categories`
- `GET http://localhost:8000/docs` - FastAPI 自動文件

**完成標準:**
- ✅ 所有 API 端點正常運作
- ✅ 資料正確回傳
- ✅ 關聯資料完整 (類別、顏色等)
- ✅ API 文件自動生成

---

## 🎯 Phase 3: AI/NLU 整合 (AI Layer)

**目標**: 整合 Ollama LLM 和語義搜尋，實現智能推薦

### ⏳ Task 3.1: 安裝 Ollama
- 下載並安裝 Ollama: https://ollama.ai/
- 下載 Llama 3.1 模型
  ```powershell
  ollama pull llama3.1:8b
  ollama list  # 確認模型已下載
  ```

### ⏳ Task 3.2: 建立 Ollama Service
- `services/ollama_service.py`
  - `chat()` - 呼叫 Ollama API
  - `parse_json_response()` - 解析 LLM JSON 回應

### ⏳ Task 3.3: 建立 NLU Service (意圖解析)
- `services/nlu_service.py`
  - `parse_intent(user_input)` - 解析使用者意圖
    - 精確搜尋: "我想找白色襯衫"
    - 模糊搜尋: "適合夏天的洋裝"
    - 風格搜尋: "正式商務風格"
    - 場合搜尋: "約會穿搭推薦"
  - 返回 `ParsedIntent` (intentType, entities, filters)

### ⏳ Task 3.4: 建立 Embedding Service (語義向量)
- 安裝 Sentence Transformers
  ```powershell
  pip install sentence-transformers
  ```
- `services/embedding_service.py`
  - 使用 `paraphrase-multilingual-MiniLM-L12-v2` (384維)
  - `encode(text)` - 生成文本向量
  - `batch_encode(texts)` - 批次生成向量

### ⏳ Task 3.5: 生成商品向量
- `scripts/generate_embeddings.py`
  - 為所有商品生成向量
  - 向量來源: `productDisplayName + articleType + baseColour + season + usage`
  - 儲存到 `product_embeddings` 表

### ⏳ Task 3.6: 建立 Recommendation Repository
- `repositories/recommendation_repository.py`
  - `exact_search(entities)` - 精確搜尋
  - `semantic_search(query_vector, top_k)` - 語義搜尋
  - `style_based_search(filters, limit)` - 風格搜尋

### ⏳ Task 3.7: 建立 Recommendation Service
- `services/recommendation_service.py`
  - `recommend(query, limit)` - 主要推薦邏輯
    1. 呼叫 NLU Service 解析意圖
    2. 根據意圖類型選擇搜尋策略
    3. 呼叫對應的 Repository 方法
    4. 生成推薦理由 (使用 LLM)
    5. 返回 `ProductRecommendation[]`

### ⏳ Task 3.8: 建立推薦 API
- `api/recommendation_api.py`
  - `POST /api/v1/recommend`
    - Request: `{"query": "我想找適合約會的洋裝", "limit": 10}`
    - Response: `ProductRecommendation[]` (含 matchScore, reason)
- `schemas/recommendation_schema.py`
  - `RecommendationRequest`
  - `RecommendationResponse`
  - `ProductRecommendation`
  - `ParsedIntent`

### ⏳ Task 3.9: 測試推薦功能
測試不同類型的查詢：
- 精確搜尋: `{"query": "白色襯衫", "limit": 5}`
- 風格搜尋: `{"query": "正式商務風格", "limit": 10}`
- 場合搜尋: `{"query": "約會穿搭推薦", "limit": 10}`
- 語義搜尋: `{"query": "適合夏天海邊的服飾", "limit": 10}`

**完成標準:**
- ✅ NLU 能正確識別不同意圖類型
- ✅ 語義搜尋返回相關商品
- ✅ 風格搜尋推薦符合需求的商品
- ✅ 每個推薦都有合理的推薦理由

---

## 🎯 Phase 4: Frontend 開發 (UI Layer)

**目標**: 建立 React 前端介面，提供良好的使用者體驗

### ⏳ Task 4.1: 建立 React 專案
```powershell
cd ..
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
```

### ⏳ Task 4.2: 安裝依賴
```powershell
# UI Framework
npm install tailwindcss postcss autoprefixer
npx tailwindcss init -p

# State Management
npm install zustand

# HTTP Client
npm install axios

# UI Components
npm install lucide-react
```

### ⏳ Task 4.3: 設定 Tailwind CSS
- 設定 `tailwind.config.js`
- 設定 `index.css`
- 建立設計系統 (顏色、字型、間距)

### ⏳ Task 4.4: 建立專案結構
```
frontend/src/
├── components/
│   ├── SearchInput/      # 搜尋輸入框
│   ├── ProductCard/      # 商品卡片
│   ├── ProductList/      # 商品列表
│   ├── ProductDetail/    # 商品詳情
│   ├── FilterPanel/      # 篩選面板
│   └── Cart/             # 購物車
├── pages/
│   ├── HomePage.tsx
│   ├── ProductDetailPage.tsx
│   └── CartPage.tsx
├── services/
│   └── api.ts            # API 客戶端
├── store/
│   └── index.ts          # Zustand store
├── types/
│   └── index.ts          # TypeScript 類型
└── App.tsx
```

### ⏳ Task 4.5: 建立 API Service
- `services/api.ts`
  - Axios 實例設定
  - `productApi.getList()`
  - `productApi.getDetail(id)`
  - `productApi.search(query)`
  - `recommendApi.recommend(query)`

### ⏳ Task 4.6: 建立 Zustand Store
- `store/index.ts`
  - 商品列表狀態
  - 搜尋查詢狀態
  - 購物車狀態
  - Loading 狀態
  - 錯誤狀態

### ⏳ Task 4.7: 建立 UI 組件
- `SearchInput` - 搜尋輸入框 (支援自然語言)
- `ProductCard` - 商品卡片 (圖片、名稱、價格、標籤)
- `ProductList` - 商品列表 (Grid 佈局)
- `ProductDetail` - 商品詳情 (完整資訊)
- `FilterPanel` - 篩選面板 (類別、顏色、價格等)
- `Cart` - 購物車組件

### ⏳ Task 4.8: 建立頁面
- `HomePage` - 首頁 (搜尋框 + 商品列表)
- `ProductDetailPage` - 商品詳情頁
- `CartPage` - 購物車頁面

### ⏳ Task 4.9: 整合 Backend API
- 設定環境變數 `.env`
  ```
  VITE_API_BASE_URL=http://localhost:8000/api/v1
  ```
- 測試 API 串接

### ⏳ Task 4.10: 啟動 Frontend
```powershell
npm run dev
# 訪問 http://localhost:5173
```

### ⏳ Task 4.11: 完整測試
- 搜尋功能正常
- 商品列表顯示正確
- 商品卡片點擊進入詳情頁
- 篩選功能正常
- 購物車功能正常
- Loading 狀態顯示
- 錯誤處理正常
- 響應式設計 (手機/平板/桌面)

**完成標準:**
- ✅ 前端與後端 API 正常溝通
- ✅ 搜尋體驗流暢
- ✅ 商品資訊顯示完整
- ✅ UI 美觀且響應式
- ✅ 購物車功能完整

---

## 📊 進度追蹤

### Phase 1: 資料層 ⏳ 準備開始
- 專案結構建立
- Database Models 建立
- Fashion Dataset 資料匯入
- 資料驗證

### Phase 2: Backend API ⏳ 等待中
- Repository Layer
- Service Layer
- API Endpoints
- API 測試

### Phase 3: AI 整合 ⏳ 等待中
- Ollama 設定
- NLU Service
- Embedding Service
- 推薦引擎
- 推薦 API

### Phase 4: Frontend ⏳ 等待中
- React 專案設定
- UI 組件開發
- API 整合
- 完整測試

**總預計時間**: 14-19 天 (MVP)  
**已完成時間**: 0 天

---

## 🎯 當前狀態

**目前進度**: 準備開始 Phase 1

**已完成項目:**
- ✅ 需求文件撰寫
- ✅ Fashion Dataset 下載與解壓
- ✅ 資料結構分析

**下一步**: 開始 Phase 1 - Task 1.1 (建立專案結構)

**準備事項:**
- 安裝 Python 3.10+
- 安裝 Node.js 18+
- 安裝 Ollama (Phase 3 使用)
- 確認 Fashion Dataset 位置: `fashion-dataset/`

---

**文件版本**: 1.0  
**建立日期**: 2025-11-19  
**最後更新**: 2025-11-19
